/** Último template guardado con Save en esta carpeta de trabajo (sobrevive recargas). */
export function promptTemplateStorageKey(work: string): string {
  return `videomaker:prompt-template:${work}`;
}
