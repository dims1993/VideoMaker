# Coqui TTS: Python >=3.9 y <3.12. En Mac, NO pegues varios comandos en una sola línea (pulsa Enter entre cada uno).
#
# Instalación del venv (usa "uv" + CPython 3.11 embebido; evita fallos de Homebrew/pyexpat):
#   curl -LsSf https://astral.sh/uv/install.sh | sh   # una vez, si no tienes uv
#   bash scripts/setup_venv.sh
#   source .venv/bin/activate
# Tras activar, usa:  python -m pip install …

# Core pipeline
python-dotenv>=1.0.0
requests>=2.31.0
anthropic>=0.25.0
streamlit>=1.28.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9
youtube-transcript-api>=1.0.0
psycopg[binary]>=3.2.0
redis>=5.0.0
celery>=5.4.0

# Video / audio editing (MoviePy 2 cambió la API; el montaje actual usa 1.x)
moviepy>=1.0.3,<2
numpy>=1.24.0
# Clonación por MP3 en la UI: librosa + ffmpeg en PATH (Mac: brew install ffmpeg)
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.1

# Subtítulos (local, gratis) — instala aparte si falla el build: pip install openai-whisper
openai-whisper>=20231117

# ⓍTTS v2 / Coqui (pesado; requiere torch). Tras activar el venv:
#   pip install torch  # desde pytorch.org según CPU/CUDA
#   pip install TTS
#
# Coqui TTS 0.22.x + `pip install TTS` suele dejar transformers 5.x → rompe XTTS (BeamSearchScorer).
# Repara el venv (versiones probadas con TTS 0.22.0):
#   python -m pip install "transformers==4.37.2" "tokenizers==0.15.2"

# Bark (alternativa expresiva; también pesado)
# pip install git+https://github.com/suno-ai/bark.git
